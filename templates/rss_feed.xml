<?xml version="1.0" encoding="UTF-8"?>
<rss version="2.0">
<channel>
  <title>{{ category.name }} - RSS Feed</title>
  <link>{{ request.url_root }}rss-feed/category/{{ category.id }}</link>
  <description>Latest posts in {{ category.name }}</description>
  <language>en-us</language>

  {% for post in posts %}
  <item>
    <title>{{ post.title }}</title>
    <description><![CDATA[{{ post.summary }}]]></description>
    <link>{{ request.url_root }}{{ category.slug }}/{{ post.slug }}</link>
    <pubDate>{{ post.created_at.strftime('%a, %d %b %Y %H:%M:%S') }} GMT</pubDate>
  </item>
  {% endfor %}
</channel>
</rss>
